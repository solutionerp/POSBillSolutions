<UserControl x:Class="RestaurantRetailPOSBill.WPF.Views.POSLoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RestaurantRetailPOSBill.WPF.Views" xmlns:viewmodels="clr-namespace:RestaurantRetailPOSBill.WPF.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:POSLoginViewModel}"
              mc:Ignorable="d" 
             Background="AliceBlue"
             d:DesignHeight="800" d:DesignWidth="600">
    
    <Grid Background="LightPink">
        <Border  Width="300" Height="300" BorderBrush="#FF3E3E3E" BorderThickness="2" CornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="10*"></RowDefinition>
                    <RowDefinition Height="10*"></RowDefinition>
                    <RowDefinition Height="10*"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10*"></ColumnDefinition>
                        <ColumnDefinition Width="10*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" HorizontalAlignment="Center"  VerticalAlignment="Center" Text="{Binding Username,UpdateSourceTrigger=PropertyChanged}">USER NAME</TextBlock>
                    <TextBox x:Name ="txtUserName" Grid.Column="1" HorizontalAlignment="Center" Height="30" Width="100" VerticalAlignment="Center" Text="{Binding Username}" ></TextBox>
                </Grid>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10*"></ColumnDefinition>
                        <ColumnDefinition Width="10*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" >PASSWORD</TextBlock>
                    <TextBox x:Name ="pbPassword" Grid.Column="1" HorizontalAlignment="Center" Height="30" Width="100" VerticalAlignment="Center" Text="{Binding Password}" ></TextBox>
                </Grid>
                <Grid Grid.Row="2">
                    <Button HorizontalAlignment="Center" Height="30" Width="100"  VerticalAlignment="Center" Click="Login_Click" >LOGIN</Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl >